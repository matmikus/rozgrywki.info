mutation update_userSettings(
    $userId: String,
    $settings: json
) {
    update_userSettings(_set: {
        settings: $settings
    }, where: {userId: {_eq: $userId}) {
        affected_rows
    }
}