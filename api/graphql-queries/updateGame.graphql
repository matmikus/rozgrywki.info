mutation (
    $id: Int,
    $aResult: smallint,
    $bResult: smallint,
    $date: date
) {
    update_games(_set: {
        aResult: $aResult,
        bResult: $bResult,
        date: $date
    }, where: {id: {_eq: $id}}) {
        affected_rows
    }
}
