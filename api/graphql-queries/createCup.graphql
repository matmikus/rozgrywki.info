mutation (
    $competitionId: Int,
    $isDoubleGame: Boolean,
    $size: smallint
) {
    insert_cups(objects: [{
        competition: $competitionId,
        isDoubleGame: $isDoubleGame,
        size: $size
    }]) {
        affected_rows
    }
}
